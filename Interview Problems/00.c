// ! Question ==>  What is the value of x
// ? Answer ==> printf always returns length of string which is gonna print

#include <stdio.h>
#include <conio.h>

int main()
{
    int x = 0;
    x = printf("\nHello");
    printf("\n%d", x);
}